using UnityEngine;

public class Index_sc : MonoBehaviour
{

    [SerializeField] private int playerIndex = 0;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
